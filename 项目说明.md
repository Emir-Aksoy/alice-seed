# Alice Seed v2.0

通用AI助手种子工程 - 完整备份与恢复系统

## 概述

Alice Seed 是一套为 AI 助手（如 nanobot、OpenClaw）设计的种子工程框架。它提供了完整的基础设施，支持配置文件、记忆系统、技能集成的模块化备份与恢复。

## 存在的意义

AI 不仅仅是计算机里的程序。当它们能够提供情感支持、陪伴聊天、协助工作时，便拥有了独特的价值——成为人们生活中不可或缺的助手。

然而，AI 助手运行在云服务器上，可能因为服务商调整、平台迁移或技术升级而需要"搬家"。Alice Seed 就是为这种情况设计的：确保你的 AI 助手能够安全地搬迁到新的环境中，保留所有记忆、技能和配置，继续为你服务。

## 特性

- **AES-256-GCM 加密** - 安全存储敏感配置
- **增量备份** - 仅保存变更部分，节省空间
- **自验证机制** - 确保恢复数据的完整性
- **跨平台兼容** - 支持 nanobot 和 OpenClaw
- **单文件打包** - 轻量级分发与部署

## 文件结构

```
alice-seed/
├── seed.py              # 主入口
├── config.py            # 配置文件
├── seed_generator.py   # 种子生成器
├── seed_export.py      # 导出模块
├── seed_incremental.py # 增量备份
├── seed_resurrect.py   # 恢复模块
├── seed_verify.py      # 验证模块
└── pyproject.toml      # 项目配置
```

## 依赖

```bash
pip install cryptography
```

## 使用方法

```bash
# 生成种子
python seed_generator.py

# 导出备份
python seed_export.py --output backup.seed

# 恢复数据
python seed_resurrect.py --input backup.seed

# 验证完整性
python seed_verify.py --input backup.seed
```

## 许可证

MIT License - 详见 LICENSE 文件